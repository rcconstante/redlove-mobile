# Public backend origin used by the Expo mobile app.
# Do not include backend-only secrets such as DATABASE_URL, SESSION_SECRET,
# PAYPAL_CLIENT_SECRET, or AI provider keys in the mobile app.
#
# The app automatically appends /api when needed.
EXPO_PUBLIC_API_URL=https://your-backend-domain.com

